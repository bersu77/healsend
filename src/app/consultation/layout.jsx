export const metadata = {
  title: "Consultation | HealSend",
  description:
    "Consultation access inside the HealSend experience.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function ConsultationLayout({ children }) {
  return children;
}
