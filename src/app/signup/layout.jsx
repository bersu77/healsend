export const metadata = {
  title: "Sign Up | HealSend",
  description:
    "Create your HealSend account to continue onboarding, treatment tracking, and follow-up care.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function SignupLayout({ children }) {
  return children;
}
