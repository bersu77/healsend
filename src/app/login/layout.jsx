export const metadata = {
  title: "Login | HealSend",
  description:
    "Sign in to your HealSend account or authorized dashboard access through the shared HealSend login flow.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function LoginLayout({ children }) {
  return children;
}
