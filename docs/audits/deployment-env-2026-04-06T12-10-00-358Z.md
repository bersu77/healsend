# Deployment Environment Audit

- Generated at: `2026-04-06T12:10:00.358Z`
- Target: `production`
- External connectivity checks: `disabled`
- Pass: `8`
- Warn: `1`
- Fail: `0`

The configured environment passed the current deployment-readiness audit. Remaining cutover work is manual rollout, monitoring, and rollback execution.

## Checks

- [PASS] `NEXT_PUBLIC_APP_URL` — Using https://healsend.com.
- [PASS] `STRIPE_SECRET_KEY` — Stripe secret key looks configured (live).
- [PASS] `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` — Stripe publishable key looks configured (live).
- [PASS] `STRIPE_WEBHOOK_SECRET` — Stripe webhook secret is set.
- [PASS] `Google OAuth` — Google OAuth credentials are configured.
- [PASS] `Apple OAuth` — Apple OAuth credentials are configured.
- [PASS] `GHL_API_KEY` — GoHighLevel API key is configured.
- [WARN] `GHL OAuth` — GHL OAuth is disabled in this environment.
  Action: If you depend on the OAuth flow, set real GHL_CLIENT_ID and GHL_CLIENT_SECRET and re-enable it.
- [PASS] `Uploads` — The app uses local filesystem uploads under public/uploads and does not require extra upload env vars.
  Action: Still verify runtime upload/write permissions in the deployed environment.
