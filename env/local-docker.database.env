# Connection string for local Postgres from repo-root `docker-compose.yml`
# (postgres:16-alpine; user/password/db match docker-compose.yml)
# Used by: `npm run db:bootstrap:docker` and `seed-onboarding-templates.mjs --with-local-docker`
DATABASE_URL=postgresql://johndoe:randompassword@localhost:5433/mydb?schema=public
